import {
  Button,
  Flex,
  Box,
  Spacer,
  Heading,
  useColorMode,
  useColorModeValue,
} from "@chakra-ui/react";

const projects = () => {
  return (
    <div>
      <p>projects</p>
    </div>
  );
};

export default projects;
